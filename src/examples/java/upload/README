Upload server and client
========================

The client streams a file from disk to an HTTP request.

The server reads the HTTP request and streams the data to a file on disk

To run the server: ./upload_server.sh

To run the http client ./upload_client.sh