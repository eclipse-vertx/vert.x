To run this example:

1) Make sure you have a local rabbitmq instance running on default host and port
2) Make sure you have a local redis instance running on default host and port
3) Start a STOMP server
In src/examples/java/stomp :
./stomp_server.sh
4) Run the example ./composition_example.rb
5) In a browser go to http://localhost:8080
6) Select an item and hit submit
