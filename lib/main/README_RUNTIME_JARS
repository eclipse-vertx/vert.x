Jars needed at run-time:

Core vert.x needs the following jars:

netty.jar
jackson-core.jar
jackson-mapper.jar

If you're using clustering you also need:

hazelcast.jar

If you have ruby verticles:

jruby.jar

If you have Groovy verticles:

groovy.jar

If you have JavaScript verticles:

js.jar